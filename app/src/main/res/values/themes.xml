<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Suflyor" parent="android:Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:colorAccent">#FFFFB020</item>
    </style>

    <!-- The tile's trampoline: nothing visible except the microphone permission dialog when it is needed. -->
    <style name="Theme.Suflyor.Invisible" parent="android:Theme.DeviceDefault.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:colorAccent">#FFFFB020</item>
    </style>
</resources>
